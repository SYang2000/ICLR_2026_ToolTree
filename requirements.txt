openai>=1.0.0
huggingface_hub>=0.20.0
transformers>=4.36.0
torch>=2.0.0
numpy>=1.24.0
tqdm>=4.65.0
pyyaml>=6.0
requests>=2.31.0
rank-bm25>=0.2.2

# Optional — only needed to host the planner/judge model locally
# vllm>=0.6.0
